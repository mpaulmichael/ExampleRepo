using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Glide02 : MonoBehaviour
{
    public GameObject makeanewcubenow;
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        if (transform.position.x >= 0)
        {
           // Instantiate (makenewcubenew, new Vector3(Random.Range(0f,10.0f),0,0), Quaternion.identity);
        }
        
        
    }
}
